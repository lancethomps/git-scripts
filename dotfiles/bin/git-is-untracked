#!/usr/bin/env bash
################################################################### SETUP ########################################################################
set -o errexit -o errtrace -o nounset
##################################################################################################################################################

if git untracked --error-unmatch -- "$@" >/dev/null 2>&1; then
  exit 0
else
  exit 1
fi
